/* styles.css */

body {
    margin: 0;
    padding: 0;
}

.container {
    width: 80%;
    margin: 0 auto;
}

header {
    font-style:'Times New Roman', Times, serif;
    background-color: #e04b5a;
    color: rgb(255, 255, 255)55, 255, 255;
    padding: 15px 0;
    text-align: center;
}

nav ul {
    font-variant: small-caps;
    font-family: cursive;
    font-weight: 1000;
    list-style-type: armenian;
    margin: 0;
    padding: 0;
    display: flex;
}

nav ul li {
    display: inline-block;
    margin-right: 70px;
    flex: auto;
}

nav a {
    text-decoration: none;
    color: rgb(226, 231, 207);
    position: relative;
}

nav a::after {
    content: '';
    display: block;
    width: 0;
    height: 3px;
    background-color: #88e0d0;
    position:relative;
    bottom: 0;
    transition: width 0.3s ease-in-out;
}

nav a:hover::after {
    width: 100%;
}


.hero {
    background-image: url('Hair-Salon-Logo-Design.jpg'); /* Replace with your background image path */
    background-size: cover;
    background-position: center;
    color: rgb(10, 9, 9);
    text-align: center;
    font-family: Georgia, 'Times New Roman', Times, serif;
    padding: 80px 0;
    position: relative;
}

.hero::before {
    content: "";
    position: absolute;
    top: 0;
    right: 0;
    bottom: 0;
    left: 0;
    background: inherit;
    filter: blur(5px); /* Adjust the blur strength as needed */
}

.hero h2 {
    font-size: 2.5em;
    margin-bottom: 20px;
    position: relative;
    z-index: 1;
}

.button {
    font-style: oblique;
    display: inline-block; 
    padding: 12px 20px;
    background-color: #d56074;
    color: white;
    text-decoration: none;
    border-radius: 5px;
    font-size: 1em;
    transition: background 0.3s linear;
    position: relative;
    z-index: 2;
}

.button:hover {
    background-color: #9effa9c7;
    color: #1a1a1a;
}

.services {
    position: relative;
    padding: 30px 0;
    overflow: hidden; /* Ensure the blur effect doesn't extend beyond the section */
}

.services::before {
    content: "";
    position: fixed;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    background-image: url('Hair-Salon-Logo-Design.jpg');
    background-size: auto;
    background-position: center;
    filter: blur(5px); /* Adjust the blur value as needed */
    z-index: -1; /* Place the blurred image behind the content */
}

.service-item {
    position: relative;
    display: block;
    flex-direction: column;
    align-items: center;
    text-align: center;
    margin-bottom: 40px;
    background: linear-gradient(45deg, #da6969, #e49756);
    padding: 20px;
    border-radius: 8px;
    box-shadow: 0 0 10px rgba(0, 0, 0, 1);
    height: 300px;
}

.service-item img {
    max-width: 100%;
    height: 150px;
    object-fit: cover;
    border-radius: 5px;
    margin-bottom: 15px;
}

.service-item h3 {
    z-index: 1; /* Place the text above the image */
    transition: color 0.3s ease-in-out, opacity 0.3s ease-in-out, text-shadow 0.3s ease-in-out; /* Adding multiple transitions for a professional effect */
}

.service-item:hover h3 {
    color: #88e0d0; /* Your hover text color */
    opacity: 0.8; /* Reduce opacity on hover for a subtle effect */
    text-shadow: 1px 1px 2px rgba(0, 0, 0, 0.5); /* Add a subtle text shadow on hover */
}
.service-item img {
    max-width: 100%;
    height: 150px;
    object-fit: cover;
    border-radius: 5px;
    margin-bottom: 15px;
    transition: transform 0.3s ease-in-out;
}

.service-item img:hover {
    transform: scale(1.1);
}




.contact {
    background-image: url('Hair-Salon-Logo-Design.jpg'); /* Replace with your background image path */
    background-size: cover; 
    background-position: center;
    background-attachment: fixed; 
    padding: 30px 0;
}

.contact address {
    margin-top: 20px;
}

.book-now {
    position: relative;
    z-index: 1;
    padding: 50px 0;
    color: #fff;
    text-align: center;
    background-color: rgba(218, 105, 105, 0.8);
    border-radius: 15px;
    overflow: hidden; /* Ensure the blur effect doesn't extend beyond the section */
}

.book-now::before {
    content: "";
    position: absolute;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    background-image: url('Hair-Salon-Logo-Design.jpg');
    background-size: auto;
    background-position: center;
    filter: blur(5px); /* Adjust the blur value as needed */
    z-index: -1; /* Place the blurred image behind the content */
}

.book-now form {
    max-width: 400px;
    margin: 0 auto;
    background-color: #fff;
    padding: 30px;
    border-radius: 15px;
    box-shadow: 0 0 20px rgba(0, 0, 0, 0.3);
}

.book-now label {
    display: block;
    margin-bottom: 15px;
    color: #333;
}

.book-now input,
.book-now select {
    width: 100%;
    padding: 15px;
    margin-bottom: 20px;
    box-sizing: border-box;
    border: 1px solid #ddd;
    border-radius: 5px;
}

h2{
    font-family:'Franklin Gothic Medium', 'Arial Narrow', Arial, sans-serif;
    color: #1a1a1a;
    size: 50px;
}
p{
    color: #1a1a1a;
    font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
    font-weight: 500;
}
.book-now input:focus,
.book-now select:focus {
    outline: none; /* Remove the default focus outline for a cleaner look */
    border-color: #e04b5a; /* Change border color on focus for better feedback */
}

.book-now button {
    width: 100%;
    padding: 15px;
    background-color: #e04b5a; /* Use the same color as the text for the button */
    color: #fff;
    border: none;
    border-radius: 5px;
    cursor: pointer;
    transition: background-color 0.3s ease-in-out;
}

.book-now button:hover {
    background-color: #9effa9c7; /* Darken the button color on hover */
}


footer {
    background-color: #1a1a1a;
    color: white;
    text-align: center;
    padding: 30px 0;
    position: relative;
    bottom: 0;
    width: 100%;
}

.image-grid {
    display: grid;
    grid-template-columns: repeat(auto-fill, minmax(200px, 1fr));
    gap: 15px;
    background-color: #ebaaaa; /* Add a light background color */
    padding: 20px; /* Add padding to the grid container */
    border-radius: 15px; /* Add border radius for rounded corners */
    box-shadow: 0 0 20px rgba(0, 0, 0, 0.1); /* Add a subtle box shadow */
}

.image-grid a {
    position: relative;
    display: block;
    overflow: hidden;
    border-radius: 15px; /* Match the border radius of the grid container */
}

.image-grid img {
    width: 100%;
    max-width: 100%;
    height: auto;
    transition: transform 0.3s ease-in-out;
    border-radius: 10px; /* Add border radius for rounded corners */
    border: 2px solid #0b0707;
    box-shadow: 0 0 10px rgba(0, 0, 0, 0.3);
}

.image-grid a:hover img {
    transform: scale(1.1);
    border: 2px solid #ff7f50;
}

.gallery {
    text-align: center;
    padding: 50px 0;
}

.gallery h2 {
    font-size: 2em;
    margin-bottom: 20px;
    color: #333; /* Adjust the text color */
}

/* Add a border around the entire gallery */
.gallery-border {
    border: 2px solid #a55a5a; /* Choose a color for the border */
    border-radius: 15px; /* Match the border radius of the grid container */
    overflow: hidden;
    display: inline-block;
}




/* Responsive Design */
@media only screen and (max-width: 768px) {
    .container {
        width: 90%;
    }

    .hero {
        padding: 60px 0; /* Adjusted padding for smaller screens */
    }

    .button {
        font-size: 0.9em; /* Adjusted font size for smaller screens */
    }
}
